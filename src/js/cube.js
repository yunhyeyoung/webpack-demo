const volume = x => x * x * x;
// CommonJs 전체 모듈화 module.exports = 전체내용
// => es 모듈에서는 export default 전체
export default volume;

/*
export  default {
  //shorthand proper 예) {player: player}
  volume,
  area

};*/
